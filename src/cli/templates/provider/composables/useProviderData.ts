import { computed } from "vue";
import { reactives as {{EntityName}}Reactives } from '../{{EntityNameLowercase}}.memory'

export const entitiesList = computed(() => {{EntityName}}Reactives.entitiesList)

